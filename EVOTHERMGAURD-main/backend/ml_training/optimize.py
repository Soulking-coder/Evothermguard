"""NSGA-II is deliberately opt-in: run only against a configured labelled validation workflow."""
def main(): print("No optimisation run: configure objective evaluation over labelled validation data first.")
if __name__=='__main__': main()
